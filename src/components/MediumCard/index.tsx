import {FC, memo} from 'react';
import {Container} from './styles';
import {Props} from './types';

const MediumCard: FC<Props> = ({}) => {
     return (
          <Container>
              
          </Container>
     );
};

export default memo(MediumCard);

import { FC, memo } from 'react';
import { Container, MainContainer } from './styles';

const BigCard: FC = () => {
     return (

          <MainContainer>
               <Container />
          </MainContainer>)
};

export default memo(BigCard);

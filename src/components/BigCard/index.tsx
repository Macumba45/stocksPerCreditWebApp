import {FC, memo} from 'react';
import {Container} from './styles';

const BigCard: FC = () => {
     return (
          <Container>
              
          </Container>
     );
};

export default memo(BigCard);
